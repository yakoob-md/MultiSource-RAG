My RAG system returns 0 results silently. Fix the following in backend/rag/retriever.py and backend/vectorstore/faiss_store.py:
Problem 1: Silent FAISS failure
In faiss_store.py, the _get_index() function loads silently. Add:

If FAISS_INDEX_PATH doesn't exist, log a clear warning: "[FAISS] No index found at {path}. Ingest documents first."
Add a get_stats() function returning {"total_vectors": int, "index_path": str, "exists": bool}

Problem 2: retrieve() swallows errors
In retriever.py, the main retrieve() function has no error boundaries. Wrap each step (embed, search, fetch MySQL, rerank) in try/except and log which step failed.
Problem 3: Add a test endpoint
In backend/api/query.py, add a GET endpoint /query/debug that:

Embeds the string "test query"
Calls search_vectors with top_k=3
Returns what FAISS found (chunk IDs and scores)
Also queries MySQL for those chunk IDs
Returns full diagnostic JSON

Problem 4: Fix the generate_multi_answer empty-chunks case
In backend/rag/multi_generator.py, if result.all_chunks is empty, the current message is generic. Change it to:
python"I searched your knowledge base but found no relevant information. 
 This usually means: (1) no documents have been ingested yet, 
 (2) your question doesn't match any uploaded content, or 
 (3) the FAISS index is empty. 
 Please upload a PDF or website first, then try again."
Problem 5: Fix multi_retrieve fallback
In backend/rag/multi_retriever.py, retrieve_single_source() calls retrieve(question, source_ids=source_ids) but the signature is retrieve(question, source_ids). Verify argument names match retriever.py exactly. If not, fix the call.

