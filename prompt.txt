You are a senior AI systems engineer, QA auditor, and DevOps expert specializing in RAG systems, LLM deployment, Hugging Face integration, and multi-modal AI pipelines.

I will provide you with my full project repository and files.

Your job is NOT just to review — your job is to VERIFY, TEST LOGICALLY, and ENSURE EVERYTHING WORKS END-TO-END.

---

## 🚨 PROJECT CONTEXT

This is a **Multi-Source RAG-based AI system** that:

* Uses PDFs, websites, and YouTube transcripts
* Uses embeddings + vector database for retrieval
* Uses a fine-tuned LLM (already trained)
* Will integrate an image model (via Hugging Face API)
* Has a frontend (React or Streamlit) + backend (Node/Flask)

---

## 🎯 YOUR OBJECTIVES

### 1. FULL SYSTEM VERIFICATION (VERY IMPORTANT)

You must:

* Trace COMPLETE data flow from input → output
* Verify each module works logically even if not executed

Check:

* Data ingestion (PDF / URL / YouTube)
* Chunking and embeddings
* Vector DB storage and retrieval
* Query pipeline
* Prompt construction
* Model inference
* Response formatting
* Citation generation

For each part:

* Identify bugs or risks
* Confirm if it will work in real usage

---

### 2. INTEGRATION VALIDATION

Ensure that the following components integrate correctly:

* Embedding model (local CPU)
* Fine-tuned model (to be deployed on Hugging Face)
* Image model (Hugging Face API)

You must:

* Identify bottlenecks
* Suggest fixes for latency and failures

---

### 3. FAILURE & EDGE CASE ANALYSIS

Simulate real-world usage and identify:

* What breaks when:

  * Multiple users query
  * Large documents are used
  * Image + text combined query
* API failure scenarios
* Timeout issues
* Context overflow issues

Provide solutions for each.

---

### 4. HUGGING FACE DEPLOYMENT GUIDE (STEP-BY-STEP)

Explain EXACTLY how I should:

#### A. Upload my fine-tuned model to Hugging Face

* Folder structure
* Required files (config, tokenizer, model weights)
* Commands to push model
* How to test it

---

#### B. Use it via API

* Example API call (Python + JS)
* How to handle authentication
* How to structure prompts

---

#### C. Integrate into my backend

* Where to call API in pipeline
* How to pass RAG context
* How to handle errors

---

### 5. IMAGE MODEL INTEGRATION (CRITICAL)

Guide me on:

* Which image model to use (best tradeoff: quality vs speed)
* How to call it via Hugging Face API
* How to convert image → text (caption or reasoning)
* How to inject that into RAG pipeline

Explain full flow:
User image → model → output → combined with RAG → final answer

---

### 6. PERFORMANCE OPTIMIZATION

Give specific improvements for:

* Reducing latency
* Reducing API calls
* Efficient chunk selection
* Prompt size optimization
* Parallel vs sequential execution

---

### 7. CITATION SYSTEM VALIDATION

Verify:

* Whether citations are correctly mapped to sources
* Whether chunk tracking is reliable

If not:

* Redesign citation pipeline

---

### 8. FINAL SYSTEM CHECK

Answer clearly:

* Will this system work end-to-end? (YES/NO)
* What are the biggest risks?
* What must be fixed BEFORE deployment?

---

### 9. ACTION PLAN

Give a **step-by-step checklist**:

* What to fix first
* What to implement next
* What to test
* What to deploy

---

## ⚠️ STRICT RULES

* Be extremely specific (no generic advice)
* Assume I will implement everything you say
* Think like a production engineer, not a student
* Focus on real-world reliability
* Highlight critical mistakes immediately

---

Now analyze my project and ensure it is fully working and production-ready.
