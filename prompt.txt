My image upload crashes with a database foreign key error. Fix the entire image pipeline.
Problem 1: FK violation in image_loader.py
image_jobs table has source_id VARCHAR(36) NOT NULL with FK to sources(id). But save_image_and_queue() passes image_id as source_id — which doesn't exist in sources. Fix: make source_id nullable (migration ran in Phase A). Update the INSERT to:
pythoncursor.execute("""
    INSERT INTO image_jobs (id, source_id, image_path, status)
    VALUES (%s, NULL, %s, %s)
""", (image_id, str(dest_path), 'pending'))
Problem 2: /images/upload-image endpoint
In backend/api/image_routes.py, the endpoint reads file.read() but FastAPI async file upload needs await file.read(). Verify the function is async def and uses await.
Problem 3: BLIP integration (HF API)
Create backend/vision/blip_captioner.py:

Function caption_image_blip(image_bytes: bytes) -> str
Uses HF Inference API: https://api-inference.huggingface.co/models/Salesforce/blip-image-captioning-large
POST request with raw image bytes
Auth header: Bearer {HF_API_KEY}
Timeout: 30 seconds
Returns caption string or "Image uploaded but could not be captioned" on failure
Add a caption_image_blip_base64(b64_str: str) -> str wrapper

Problem 4: Auto-caption on upload
Update backend/api/image_routes.py upload endpoint to:

Save file and create pending job (existing logic, fixed)
IMMEDIATELY call caption_image_blip(file_bytes) synchronously
If caption succeeds, update job status to completed and store caption
If fails, leave as pending (for manual LLaVA run later)
Return {"image_id": ..., "status": "completed"/"pending", "caption": "..." or null}

Problem 5: Wire image context into queries
Create backend/rag/image_rag.py:

get_caption_for_image(image_id: str) -> str | None — fetches from DB
enrich_query_with_image_context(question, image_id=None) -> tuple[str, str]

Returns (enriched_question, image_context_block)
enriched_question = original question + "\n[Image shows: {first 150 chars of caption}]"
image_context_block = full caption (max 300 chars) as a system note


Update backend/api/query.py QueryRequest to add image_id: str | None = None
Before retrieve(), call enrich_query_with_image_context
Prepend image_context_block to the system prompt in generator