My RAG chat UI shows nothing because streaming is broken. There are TWO streaming endpoints with conflicting response formats. Fix this completely.
Problem: backend/api/stream.py sends {"token": "..."} and {"done": true} but backend/api/query.py sends {"type": "token", "content": "..."} and {"type": "done"}. The frontend streamQueryRag in api.ts tries to parse one format but receives the other.
Fix 1: Deprecate stream.py
In backend/main.py, comment out app.include_router(stream_router). Add a comment: # DEPRECATED: use /query-stream from query.py instead
Fix 2: Standardize query.py streaming format
In backend/api/query.py, the /query-stream endpoint's event_stream() generator must send EXACTLY this format:
python# Meta event (first):
yield f"data: {json.dumps({'type': 'meta', 'chatId': chat_id, 'conversationId': conv_id, 'citations': citations_out, 'retrievedChunks': chunks_out})}\n\n"

# Token events:
yield f"data: {json.dumps({'type': 'token', 'content': token})}\n\n"

# Done event (last):
yield f"data: {json.dumps({'type': 'done'})}\n\n"

# Error event (on exception):
yield f"data: {json.dumps({'type': 'error', 'message': str(e)})}\n\n"
Fix 3: Fix streamQueryRag in frontend/src/app/api.ts
The function currently checks parsed.token (old format). Update the SSE parser to:
typescriptif (parsed.type === 'error') { onError(new Error(parsed.message)); return; }
if (parsed.type === 'meta') {
  // map citations and retrievedChunks (same mapping as before)
  onMeta({ chatId: parsed.chatId, conversationId: parsed.conversationId, citations, retrievedChunks });
}
if (parsed.type === 'token' && parsed.content) { onToken(parsed.content); }
if (parsed.type === 'done') { return; }
Fix 4: Update AskAI.tsx
The onMeta callback receives conversationId now. Store it:
typescript(meta) => {
  capturedCitations = meta.citations || [];
  capturedChunks = meta.retrievedChunks || [];
  if (meta.conversationId && !convId) {
    convId = meta.conversationId;
    setActiveConvId(convId);
  }
  setSelectedChunks(capturedChunks);
}
Fix 5: Add connection error handling
Wrap the fetch call in streamQueryRag with a try/catch that checks if the server is reachable. If fetch throws TypeError: Failed to fetch, call onError(new Error("Cannot connect to server. Is the backend running on port 8000?")).