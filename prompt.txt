Act as a senior full-stack AI engineer and system architect.

You are working on an existing AI dashboard project that includes:
- A main chatbot interface (Intellex screen) with:
  - Conversational memory
  - Chat history
  - Context retention across turns
  - Knowledge base selection (PDFs, YouTube, URLs)
  - RAG-based retrieval
  - Multiple model selection (Groq, etc.)

- A separate "Legal AI" section that:
  - Currently works ONLY as a single-question system (no memory)
  - Does NOT support chat history
  - Does NOT retain context
  - Does NOT allow knowledge base selection
  - Uses a legal fine-tuned model

---

🎯 OBJECTIVE:

Transform the Legal AI section to FULLY MATCH the functionality of the main Intellex chatbot, while keeping its legal specialization.

---

📌 REQUIRED CHANGES:

### 1. Chat System Upgrade
Convert Legal AI from:
❌ Single-turn QA  
To:
✅ Multi-turn conversational chatbot

Implement:
- Persistent chat history (frontend + backend)
- Context retention across messages
- Ability to continue previous conversations

---

### 2. Memory Implementation
Add:
- Short-term memory (current conversation)
- Optional long-term memory (store past chats if needed)

Ensure:
- Model receives previous messages as context
- Token management is handled (truncate intelligently)

---

### 3. Knowledge Base Integration (CRITICAL)
Replicate EXACT functionality from Intellex:

User must be able to:
- Select PDFs
- Select YouTube videos
- Select URLs

Legal AI should:
- Use same RAG pipeline
- Use same embeddings + vector DB
- Allow filtering sources dynamically

---

### 4. Backend Changes
Unify pipeline:

Currently:
- Intellex → RAG + chat system
- Legal → isolated model call

Change Legal pipeline to:
User Query → Context Retrieval (RAG) → Model (Legal or Groq) → Response

---

### 5. Model Selection Logic

#### In MAIN Intellex screen:
❌ REMOVE Legal model from dropdown

#### In LEGAL section:
✅ Add model selection:
- Legal Fine-tuned Model
- Groq / General Model

Ensure:
- Switching models does NOT break context or memory

---

### 6. UI/UX Changes

Legal screen should now include:
- Chat-style interface (like ChatGPT)
- Message bubbles (user + AI)
- Scrollable history
- Input box with send button
- Source selector panel (PDF / YouTube / URL)
- Model selector dropdown (Legal + Groq)

---

### 7. State Management (IMPORTANT)

Implement proper state handling:
- Maintain chat session ID
- Store messages in backend (MongoDB or memory store)
- Sync frontend + backend state

---

### 8. API Design

Update backend endpoints:

POST /chat/legal
- Input:
  - message
  - chat_history
  - selected_sources
  - model_type

- Output:
  - response
  - updated_history

---

### 9. Context Handling Strategy

Implement:
- Sliding window for chat history
- Retrieval-augmented context injection
- Prevent token overflow

---

### 10. Code Requirements

Provide:
- Frontend changes (React)
- Backend changes (Node/Express or Flask)
- Example API implementation
- State management logic
- Sample request/response

---

📌 IMPORTANT INSTRUCTIONS:

- DO NOT give high-level theory — give IMPLEMENTATION CODE
- Maintain clean modular structure
- Reuse existing Intellex logic wherever possible
- Highlight what to REMOVE vs ADD
- Ensure production-level structure

---

📌 FINAL OUTPUT:

1. Architecture diagram (text-based is fine)
2. Step-by-step implementation plan
3. Full backend code changes
4. Frontend component updates
5. Example API calls
6. Explanation of memory + RAG integration

---

Now implement this transformation completely.