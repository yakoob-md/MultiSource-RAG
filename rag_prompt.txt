Phase 5 — Unified query endpoint
What to build: Update backend/api/query.py to route through the new pipeline. This is the only existing file that gets modified.

You are modifying ONE existing file in a FastAPI system.
File: backend/api/query.py

CURRENT: The /query endpoint calls retrieve() then generate_answer() directly.

CHANGES REQUIRED (surgical — minimum diff):

1. Add these imports at the top of the existing imports block:
   from backend.rag.query_classifier import classify_query
   from backend.rag.multi_retriever import multi_retrieve
   from backend.rag.multi_generator import generate_multi_answer

2. Modify the QueryRequest Pydantic model to add ONE new optional field:
   mode: str | None = None  # "auto" | "single" | "compare" | "synthesize"
   If mode is None or "auto", use the classifier. Otherwise force that mode.

3. Inside the /query endpoint function body, after the existing validation check,
   REPLACE the two lines:
       chunks = retrieve(req.question, req.source_ids)
       result = generate_answer(req.question, chunks, history=history)
   WITH these lines (preserve all existing error handling around them):
       analysis = classify_query(req.question)
       if req.mode and req.mode != "auto":
           analysis.intent = {"single": "single_source", "compare": "comparison",
                               "synthesize": "synthesis"}.get(req.mode, analysis.intent)
       multi_result = multi_retrieve(req.question, analysis)
       result = generate_multi_answer(req.question, multi_result, history=history)

4. In the response building section, add one field to the returned dict:
   "query_intent": analysis.intent

DO NOT change anything else. Do NOT touch /query-stream. Do NOT change imports
that are not listed above. Show me the full modified file.

Validation checklist:

POST /query with {"question": "What is IPC 302?", "mode": "auto"} returns query_intent: "single_source"
POST /query with {"question": "compare IPC 302 with SC ruling", "mode": "auto"} returns query_intent: "comparison"
POST /query with {"question": "anything", "mode": "compare"} forces query_intent: "comparison" regardless of classifier output
Existing streaming endpoint /query-stream is completely unchanged