🚀 PHASE A — DATASET GENERATION
🎯 Goal:

Create training data from your existing RAG system

🧩 What Claude wants (decoded)

From your file:

Read chunks from DB
Generate question using LLM
Generate answer using your RAG
Save in Alpaca format

🤖 GEMINI ROLE

👉 Write script:
scripts/generate_training_data.py

✍️ PROMPT FOR GEMINI (use this EXACT)
Writing

You are implementing a dataset generation script for a legal RAG system.

STRICT RULES:

Do NOT modify any existing file
Only create: scripts/generate_training_data.py
Follow instructions EXACTLY

REQUIREMENTS:

Connect to MySQL database
Run query:
SELECT c.chunk_text, c.unified_metadata, s.title
FROM chunks c
JOIN sources s ON c.source_id = s.id
WHERE c.chunk_type = 'legal' AND c.unified_metadata IS NOT NULL
LIMIT 2000
For each row:
Extract chunk_text, metadata, title
Implement function:
generate_question_for_chunk(text, metadata)

Call Groq API:
Prompt:
"Given this legal text, generate ONE specific factual question a person might ask.
Return ONLY the question.

Legal text:
{text[:500]}"

Return string

Implement function:
call_groq_for_ideal_answer(question, context)
Use same structured legal prompt used in generator
Return formatted legal answer
Build training sample:
{
"instruction": "You are a legal information assistant for Indian law. Answer ONLY using provided context.",
"input": f"CONTEXT:\n{context}\n\nQUESTION: {question}",
"output": answer
}
Save each entry to:
data/training/legal_rag_dataset.jsonl

(append mode, one JSON per line)

Add:
progress print every 50 samples
sleep(1) between API calls
At end:
print total examples generated

OUTPUT REQUIREMENTS:

Full working code
No placeholders
Proper imports
Error handling for API failures

After writing code:

Provide:

How to run it
Expected output file structure
Sample JSON entry
✅ AFTER GEMINI OUTPUTS
YOU TEST:

Run:

python scripts/generate_training_data.py
✔️ VALIDATION CHECK
File created? → legal_rag_dataset.jsonl
Entries present? → YES
Format correct? → YES
Answers structured? → YES