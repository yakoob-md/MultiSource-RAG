You are adding new files to a Python RAG system. Do NOT touch any existing files.

Existing system context:
- backend/config.py has: BASE_DIR, DATA_DIR, UPLOAD_DIR (all Path objects)
- backend/database/connection.py has get_connection()
- data/ directory exists at project root
- image_jobs table exists with columns: id, source_id, image_path, status, caption, error_message, created_at, updated_at

Create ONLY these two new files:

FILE 1: backend/ingestion/image_loader.py

This file handles image upload and job queuing. It does NOT run the vision model.

Define:
1. IMAGES_DIR = DATA_DIR / "images"  (create if not exists using .mkdir(parents=True, exist_ok=True))
2. ALLOWED_EXTENSIONS = {'.jpg', '.jpeg', '.png', '.webp', '.bmp', '.tiff'}

3. function: save_image_and_queue(file_bytes: bytes, filename: str, source_context: str = "") -> dict
   - Validate: Path(filename).suffix.lower() must be in ALLOWED_EXTENSIONS, else raise ValueError
   - Generate: image_id = str(uuid.uuid4())
   - Save: dest_path = IMAGES_DIR / f"{image_id}{Path(filename).suffix.lower()}"
   - Write file_bytes to dest_path
   - Insert into sources table: type='pdf' (use 'image' -- wait, sources.type is ENUM('pdf','url','youtube'). 
     IMPORTANT: Do NOT insert into sources table. Instead insert directly into image_jobs:
     id=image_id, source_id=image_id (same for now), image_path=str(dest_path), status='pending'
   - Return: {"image_id": image_id, "image_path": str(dest_path), "status": "pending", "filename": filename}

4. function: get_pending_image_jobs() -> list[dict]
   - Query: SELECT id, image_path FROM image_jobs WHERE status = 'pending' ORDER BY created_at ASC
   - Return list of row dicts

5. function: mark_job_completed(job_id: str, caption: str)
   - UPDATE image_jobs SET status='completed', caption=caption WHERE id=job_id

6. function: mark_job_failed(job_id: str, error: str)
   - UPDATE image_jobs SET status='failed', error_message=error WHERE id=job_id

FILE 2: backend/api/image_routes.py

Define:
1. router = APIRouter()

2. POST /upload-image
   - Accepts: file: UploadFile = File(...), context: str = ""
   - Read file bytes: await file.read()
   - Call save_image_and_queue(file_bytes, file.filename, context)
   - Return: {"message": "Image queued for processing", "image_id": ..., "status": "pending"}
   - On ValueError: raise HTTPException(400, detail=str(e))

3. GET /image-jobs
   - Returns all image_jobs rows: SELECT id, image_path, status, caption, error_message, created_at FROM image_jobs ORDER BY created_at DESC
   - Convert datetimes to isoformat
   - Return: {"jobs": [...]}

4. GET /image-jobs/pending-count
   - Returns: {"count": N} where N = SELECT COUNT(*) FROM image_jobs WHERE status='pending'

After creating these files, tell me the exact two lines to add to backend/main.py (import + include_router). Do not write the full main.py.