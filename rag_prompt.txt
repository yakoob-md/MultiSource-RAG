You are adding a LegalSearch page to an existing React + TypeScript frontend.

EXISTING CONTEXT:
- frontend/src/app/routes.tsx already has route registration pattern
- frontend/src/app/api.ts has queryRag() function as a pattern to follow
- frontend/src/app/types.ts has ChatMessage, Citation types

ADD TO api.ts (append, do not modify existing functions):
export async function queryLegal(question: string, sourceFilter?: string): Promise<LegalQueryResponse>
- POST to ${API_BASE}/legal-query
- Body: {question, source_filter: sourceFilter || null, language: "en"}
- Return typed response

ADD TYPE to types.ts:
export interface LegalCitation {
  document: string;
  section?: string;
  title?: string;
  text_excerpt: string;
  amendments?: string[];
  source_type: "statute" | "judgment";
  court?: string;
  date?: string;
  para?: string;
}
export interface LegalQueryResponse {
  answer: string;
  legal_basis: string;
  citations: LegalCitation[];
  retrieved_chunks: any[];
}

CREATE FILE: frontend/src/app/components/screens/LegalSearch.tsx

The UI must have:
1. Header: "Indian Legal Assistant" with subtitle "Powered by IPC, CrPC, Constitution, and Supreme Court Judgments"
2. Search input (large, prominent) with placeholder "Ask a legal question... e.g. 'What is the punishment for murder?'"
3. Source filter radio buttons: All | Statutes Only | Judgments Only
4. On submit: show loading state "Searching legal corpus..."
5. Answer section styled as a formal document:
   - ANSWER in a clean box
   - LEGAL BASIS in a highlighted quote box (border-left: 4px solid #6366F1)
   - CITATIONS section: each citation as a card showing:
     * Badge: "IPC" | "CrPC" | "Constitution" | "SC Judgment" (color-coded)
     * Section number + title (for statutes)
     * Case name + court + date (for judgments)
     * Text excerpt in monospace/quote style
     * If amendments present: show collapsible "Amendment History" section
6. Disclaimer at bottom: "This system provides legal information only, not legal advice. Consult a qualified advocate for specific legal matters."

Style: match existing dark/light theme (use same Tailwind classes as AskAI.tsx).
Register route /legal in routes.tsx.