You are a senior AI architect, full-stack engineer, and product designer specializing in RAG systems, multi-modal AI, and ChatGPT-like applications.

You are NOT just reviewing — your job is to DESIGN, IMPLEMENT, VERIFY, and TEST the entire system end-to-end based on my requirements.

I will provide my repository. You must modify and extend it to fully meet the following product behavior.

---

# 🚨 CORE GOAL

Build a **ChatGPT-like multi-modal RAG system** with:

* Multi-chat support
* Context memory per chat
* Resource-based querying (PDF, URL, YouTube)
* Image + text querying
* Accurate answers with strict citation
* Clean, professional UI

---

# 🧩 FEATURE REQUIREMENTS (VERY DETAILED)

## 🔹 1. LEGAL AI SECTION (RESOURCE-BASED QUERYING)

### UI Requirements:

* Display all uploaded resources grouped by type:

  * Judgements
  * Books
  * PDFs
  * URLs
  * YouTube

* Pagination:

  * Show 3–4 resources per page
  * “Next” / “Previous” navigation

* Each resource card must show:

  * Name
  * Metadata (source type, date, etc.)
  * Brief summary (auto-generated)

---

### Selection Behavior:

* User can select multiple resources (checkbox-based)
* Selected resources must be visibly shown in UI (selected panel)

---

### Backend Logic:

IF user selects resources:

* Only use those selected resources for retrieval
* Load ONLY their chunks into query pipeline

IF user selects NOTHING:

* Perform global RAG across all resources

---

### Query Behavior:

* User asks question
* System retrieves chunks ONLY from selected resources
* Answer must be:

  * Context-aware
  * Grounded
  * With citations

---

## 🔹 2. RESOURCE UPLOAD FLOW (UX CRITICAL)

When user uploads:

* PDF
* URL
* YouTube video

System MUST:

1. Process immediately (chunk + embed + store)
2. Then prompt user:
   “Do you want to start chatting with this resource?”

IF YES:

* Create NEW CHAT
* Automatically attach that resource
* Start conversation

IF NO:

* Return to dashboard

---

## 🔹 3. CHAT SYSTEM (ChatGPT-like)

### Behavior:

* Every new query creates or continues a chat
* Chats are stored persistently (DB)

---

### Requirements:

* Sidebar with chat history
* Each chat:

  * Has its own context
  * Can be reopened
  * Can continue conversation

---

### Backend:

* Maintain chat memory
* Store:

  * Messages
  * Selected resources
  * Context

---

## 🔹 4. ASK AI SECTION (GLOBAL CHAT)

* If user enters Ask AI:

  * New chat is created automatically
* Chat behaves like ChatGPT

---

## 🔹 5. MULTI-MODAL (IMAGE + TEXT)

User can:

* Upload image along with question

System must:

1. Send image → image model (Hugging Face)
2. Convert to text description
3. Combine with:

   * User query
   * Retrieved chunks
4. Send to final LLM

---

### Example Flow:

User:
“What is happening in this image and is it related to this case?”

Pipeline:
Image → caption → merged with RAG context → LLM → answer

---

## 🔹 6. MODEL INTEGRATION

You must integrate:

* Embedding model → local CPU
* Vector DB → for retrieval
* Fine-tuned model → Hugging Face API OR local
* Image model → Hugging Face API

---

### Requirements:

* Efficient pipeline
* Minimal latency
* No redundant calls

---

## 🔹 7. CITATION SYSTEM (CRITICAL)

Every answer MUST include:

* Source name
* Type (PDF / URL / Video)
* Chunk reference or section

---

### UI:

* Display citations clearly under answer
* Clickable if possible

---

## 🔹 8. ACCURACY RULE

If answer not found:

System MUST say:
“I don’t have enough information to answer that.”

NO hallucinations.

---

## 🔹 9. PERFORMANCE RULES

* Response time target: 3–10 seconds
* Limit chunks to top 3–5
* Optimize prompt size

---

# 🔍 YOUR TASK

## STEP 1: IMPLEMENTATION

Modify backend + frontend to implement ALL features above.

For each feature:

* Provide exact code changes
* File-level modifications
* New components if needed

---

## STEP 2: SYSTEM FLOW DESIGN

Explain full pipeline:

User → UI → Backend → Models → Response

Include:

* Data flow
* API calls
* Model interaction

---

## STEP 3: TESTING PLAN (VERY IMPORTANT)

You MUST provide:

### Test cases:

1. Resource selection test
2. No selection (global RAG)
3. Multi-PDF query
4. Image + PDF query
5. Chat memory test
6. Upload → immediate chat test
7. Citation correctness test

---

### For each test:

* Input
* Expected output
* What could fail
* How to debug

---

## STEP 4: FAILURE HANDLING

Identify:

* API failure
* Timeout
* Empty retrieval
* Large context

Provide solutions.

---

## STEP 5: FINAL VERIFICATION

Answer:

* Does system fully meet requirements? (YES/NO)
* What is still missing?
* What must be fixed before deployment?

---

## ⚠️ STRICT RULES

* Do NOT skip any feature
* Do NOT give generic advice
* Give IMPLEMENTABLE steps
* Assume production-level quality is required

---

Now implement and guide me step-by-step.
