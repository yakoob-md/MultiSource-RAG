You are extending a RAG system's MySQL database. The existing schema has these tables:
- sources (id, type, title, origin, language, chunk_count, status, created_at)
- chunks (id, source_id, chunk_text, chunk_index, page_number, timestamp_s, url_ref, created_at)
- chat_history (id, question, answer, sources_used, created_at)

Do NOT modify any existing table. Add ONLY new columns and tables using ALTER TABLE and CREATE TABLE IF NOT EXISTS.

Write a file: backend/database/legal_image_schema.sql

Add these changes:
1. ALTER TABLE chunks ADD COLUMN IF NOT EXISTS chunk_type ENUM('text','legal','image') DEFAULT 'text';
2. ALTER TABLE chunks ADD COLUMN IF NOT EXISTS legal_metadata JSON DEFAULT NULL;
   -- legal_metadata stores: {"case_name":"", "court":"", "date":"", "ipc_sections":[], "citation":""}
3. ALTER TABLE chunks ADD COLUMN IF NOT EXISTS image_path VARCHAR(500) DEFAULT NULL;
   -- image_path stores the disk path of the source image for image chunks

4. CREATE TABLE IF NOT EXISTS legal_sources (
   id VARCHAR(36) PRIMARY KEY,
   source_id VARCHAR(36) NOT NULL,
   doc_type ENUM('statute','judgment','constitution') NOT NULL,
   court VARCHAR(200) DEFAULT NULL,
   judgment_date DATE DEFAULT NULL,
   ipc_sections JSON DEFAULT NULL,
   petitioner VARCHAR(500) DEFAULT NULL,
   respondent VARCHAR(500) DEFAULT NULL,
   FOREIGN KEY (source_id) REFERENCES sources(id) ON DELETE CASCADE
   );

5. CREATE TABLE IF NOT EXISTS image_jobs (
   id VARCHAR(36) PRIMARY KEY,
   source_id VARCHAR(36) NOT NULL,
   image_path VARCHAR(500) NOT NULL,
   status ENUM('pending','processing','completed','failed') DEFAULT 'pending',
   caption TEXT DEFAULT NULL,
   error_message TEXT DEFAULT NULL,
   created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
   updated_at DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP
   );

Output only the SQL file. No explanations. Each statement on its own line with semicolon. Include a USE rag_system; at the top.



data/
  legal_raw/
    statutes/
      ipc_1860.pdf
      crpc_1973.pdf
      constitution.pdf
    judgments/
      sc/
        2020/
        2021/
        2022/
      hc/
  legal_processed/   ← output of legal_loader.py
  images/            ← raw uploaded images
  image_captions/    ← output of captioner.py