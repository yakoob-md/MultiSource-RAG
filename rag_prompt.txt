You are adding a new file to a Python RAG system. Do NOT modify any existing files.

Existing chunker at backend/ingestion/chunker.py has:
- chunk_text(text: str) -> list[str]  -- sentence-aware, TARGET_SENTENCES=5, MAX_CHUNK_CHARS=1200
- chunk_text_with_pages(pages: list[str]) -> list[dict with 'text' and 'page_number']

Create ONLY this new file: backend/ingestion/legal_chunker.py

Legal documents have a different structure from regular text:
- Statutes: numbered sections like "Section 302. Punishment for murder—..."
- Judgments: paragraphs numbered [1], [2], [3]... or (i), (ii)...
- They must NOT be split mid-section

The file must define:

1. chunk_statute(text: str) -> list[dict]
   - Split text at section boundaries detected by regex: r"^(Section|Sec\.?)\s+\d+[A-Z]?\." at start of line
   - Each section becomes one chunk
   - If a section text exceeds 1200 characters, split it further using existing chunk_text() from chunker.py
   - Return list of dicts: [{"text": "...", "section_number": "302", "section_title": "Punishment for murder"}]
   - Extract section_number from the section header using regex \d+[A-Z]?
   - Extract section_title as text after the section number and period, up to the em-dash or newline

2. chunk_judgment(text: str) -> list[dict]
   - Split at paragraph boundaries: paragraphs numbered [1] through [999] or blank line separating 3+ sentence blocks
   - Each paragraph chunk must be 200-800 characters
   - If a paragraph is longer than 800 characters, use existing chunk_text() to split it further
   - If a paragraph is shorter than 200 characters, merge it with the next paragraph
   - Return list of dicts: [{"text": "...", "paragraph_number": 1}]

3. chunk_legal_document(text: str, doc_type: str) -> list[dict]
   - If doc_type == 'statute': call chunk_statute(text)
   - If doc_type == 'constitution': call chunk_statute(text)  -- same structure
   - If doc_type == 'judgment': call chunk_judgment(text)
   - Always return list of dicts with at minimum the 'text' key

Import chunk_text from backend.ingestion.chunker -- use relative import or full path import.
No other imports needed except re.