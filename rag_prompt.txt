AUDIT
✅ What's Already Built (Good Work)

Hybrid retrieval (FAISS + BM25 + cross-encoder reranking)
Multi-source ingestion (PDF, URL, YouTube)
Legal RAG pipeline with specialized chunking
Streaming responses via SSE
Basic chat history (MySQL)
Image upload + LLaVA captioning queue
Fine-tuning pipeline (Kaggle + Unsloth)

⚠️ What's Broken or Missing
CRITICAL GAPS:

No persistent multi-chat system — you have one flat chat history, not ChatGPT-style "conversations"
Chat memory broken — history is sent from frontend but not stored per-conversation
Image understanding not integrated into RAG — captions exist but never flow into answers
Citations UI is weak — snippets shown but no clickable source links
/query/stream and /query-stream are DUPLICATE endpoints — one in stream.py, one in query.py — this will cause confusion
LLM provider hybrid logic — fine-tuned model integration exists but is never actually called in the main query flow


STEP 2: GAP ANALYSIS vs Your Requirements
RequirementStatusMultiple chats (sidebar)❌ MissingChat memory per conversation❌ MissingReopen old chats❌ MissingDynamic source selection✅ DoneImage + text in answer❌ Not integratedCitations in UI⚠️ PartialChatGPT-level UX❌ Needs workGrounded answers✅ Done

STEP 3: WHAT WE BUILD NOW
Here's the plan — incremental, one step at a time:
Phase 1: Multi-Chat System (Database + Backend)
Phase 2: Connect image captions to RAG
Phase 3: Fix citations UI
Phase 4: ChatGPT-like sidebar